Validate te alertin gösterilmesi ve script ile val code un doğruluğunun kontrol edilmesi

- code doğru aktarılmıyor şua n


22.01.2022
FirebasestorageHelper.GetUserFiles 
içerideki userkey parametresini değiştir


silinen func
        public async Task<List<string>> GetUserFiles(string userMail)
        {
            string userFbKey = await firebaseHelper.GetUserFBKey(userMail);
            List<StorageFileInfo> files = firebaseHelper.GetAllFiles().Result;
            List<string> userFilesList = new List<string>();
            foreach (StorageFileInfo item in files)
            {
                var a = await firebaseStorage
 .Child("-Ms11qwgtxC1vDIkHbZq")
 .Child(item.Name).GetDownloadUrlAsync();
                userFilesList.Add(a);
            }
            return userFilesList;
        }




BU projede yapılamayanlar
- klasör mantığı